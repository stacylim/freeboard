
<ListWrapper>
    번호:<span>{data && data.fetchProfile.id}</span>
    제목:<span>{data && data.fetchProfile.title}</span>
    작성자:<span>{data && data.fetchProfile.writer}</span>
    날짜:<span>{data && data.fetchProfile.date}</span>
</ListWrapper>

